# === NOME DELL'APPLICAZIONE ===
spring.application.name=ValerioBarberShopBackend

# === CONFIGURAZIONE DATABASE POSTGRESQL ===
spring.datasource.url=jdbc:postgresql://localhost:5432/barbershopdb
spring.datasource.username=postgres
spring.datasource.password=Paperino89+

# Driver PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Schema auto-update
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Pool di connessione
spring.jpa.properties.hibernate.connection.pool_size=10

# Opzionale ma consigliato

spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# === ABILITA H2 CONSOLE (opzionale finché non abbandoni H2) ===
spring.h2.console.enabled=false

# === CONFIGURAZIONE JWT ===
app.jwt.secret=dGhpcyBpcyBhIHNlY3VyZSByYW5kb20gc2VjcmV0IGtleQ== 
   # Chiave Base64-encoded
app.jwt.expiration=86400000  
  # 24 ore in millisecondi

# === PORTA DEL SERVER ===
server.port=8080